# Copia este archivo como .env y completa los valores
# El DB_HOST y DB_PASSWORD los sobreescribe docker-compose; no los cambies.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=datacl
DB_USER=root
DB_PASSWORD=

SECRET_KEY=datacl_secret_key_2026

# Clave de la API de Groq (necesaria para clasificación automática de campos)
# Solicítala a la autora del proyecto si no la tienes
GROQ_API_KEY=
