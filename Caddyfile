# Reemplaza "tu-dominio.cl" por tu dominio real cuando lo tengas.
# Caddy usara ese host para emitir HTTPS automatico con Let's Encrypt.
tu-dominio.cl {
    encode gzip

    handle_path /api/* {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:80
    }
}
